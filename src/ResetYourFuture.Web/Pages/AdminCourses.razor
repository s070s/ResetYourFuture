@page "/admin/courses"
@attribute [Authorize(Roles = "Admin")]

<PageTitle>@AdminRes.CourseManagement</PageTitle>

<h1 class="h2">@AdminRes.CourseManagement</h1>

<div class="mb-3">
    <button class="btn btn-primary" @onclick="CreateCourse">@AdminRes.CreateNewCourse</button>
</div>

@if (pagedResult == null)
{
    <LoadingSpinner Label="@AdminRes.Loading" />
}
else if (!pagedResult.Items.Any())
{
    <p>@AdminRes.NoCourses</p>
}
else
{
    <ScrollableTable Items="pagedResult.Items" TItem="AdminCourseDto">
        <HeaderRow>
            <tr>
                <SortableColumnHeader ColumnKey="title" Label="@AdminRes.FieldTitle"
                                      CurrentSortBy="@_sortBy" CurrentSortDir="@_sortDir"
                                      OnSort="OnSort" />
                <SortableColumnHeader ColumnKey="category" Label="@AdminRes.ColCategory"
                                      CurrentSortBy="@_sortBy" CurrentSortDir="@_sortDir"
                                      OnSort="OnSort" />
                <SortableColumnHeader ColumnKey="tier" Label="@AdminRes.ColTier"
                                      CurrentSortBy="@_sortBy" CurrentSortDir="@_sortDir"
                                      OnSort="OnSort" />
                <SortableColumnHeader ColumnKey="status" Label="@AdminRes.ColStatus"
                                      CurrentSortBy="@_sortBy" CurrentSortDir="@_sortDir"
                                      OnSort="OnSort" />
                <SortableColumnHeader ColumnKey="enrollments" Label="@AdminRes.ColEnrollments"
                                      CurrentSortBy="@_sortBy" CurrentSortDir="@_sortDir"
                                      OnSort="OnSort" />
                <th>@AdminRes.ColModulesLessons</th>
                <SortableColumnHeader ColumnKey="createdat" Label="@AdminRes.ColCreated"
                                      CurrentSortBy="@_sortBy" CurrentSortDir="@_sortDir"
                                      OnSort="OnSort" />
                <th>@AdminRes.ColActions</th>
            </tr>
        </HeaderRow>
        <RowTemplate Context="course">
            <tr @key="course.Id">
                <td data-label="@AdminRes.FieldTitle">@course.TitleEn</td>
                <td data-label="@AdminRes.ColCategory">@(course.CategoryNameEn ?? "—")</td>
                <td data-label="@AdminRes.ColTier">@course.RequiredTier</td>
                <td data-label="@AdminRes.ColStatus">
                    <StatusBadge IsActive="course.IsPublished"
                                 ActiveText="@AdminRes.StatusPublished"
                                 InactiveText="@AdminRes.StatusDraft" />
                </td>
                <td data-label="@AdminRes.ColEnrollments">@course.EnrollmentCount</td>
                <td data-label="@AdminRes.ColModulesLessons">@course.ModuleCount / @course.TotalLessons</td>
                <td data-label="@AdminRes.ColCreated">@course.CreatedAt.ToLocalTime().ToString("d")</td>
                <td data-label="@AdminRes.ColActions">
                    <button class="btn btn-sm btn-primary" @onclick="() => EditCourse(course.Id)">@AdminRes.Edit</button>
                    @if (course.IsPublished)
                    {
                        <button class="btn btn-sm btn-warning" @onclick="() => UnpublishCourse(course.Id)">@AdminRes.Unpublish</button>
                    }
                    else
                    {
                        <button class="btn btn-sm btn-success" @onclick="() => PublishCourse(course.Id)">@AdminRes.Publish</button>
                    }
                    <button class="btn btn-sm btn-danger" @onclick="() => DeleteCourse(course.Id)">@AdminRes.Delete</button>
                </td>
            </tr>
        </RowTemplate>
    </ScrollableTable>

    <AdminPaginationToolbar CurrentPage="pagedResult.Page"
                            TotalPages="pagedResult.TotalPages"
                            TotalCount="pagedResult.TotalCount"
                            PageSize="pagedResult.PageSize"
                            HasPreviousPage="pagedResult.HasPreviousPage"
                            HasNextPage="pagedResult.HasNextPage"
                            PageSizeOptions="PageSizeOptions"
                            ItemLabel="courses"
                            OnPageSizeChanged="OnPageSizeChanged"
                            OnPreviousPage="PreviousPage"
                            OnNextPage="NextPage" />
}

<ConfirmModal IsVisible="@(_pendingDeleteId.HasValue)"
              Title="@AdminRes.ConfirmTitle"
              Message="@AdminRes.ConfirmDeleteCourse"
              ConfirmLabel="@AdminRes.Delete"
              CancelLabel="@AdminRes.Cancel"
              OnConfirm="ExecuteDeleteAsync"
              OnCancel="@(() => _pendingDeleteId = null)" />

<DismissibleAlert Message="@message" OnDismiss="@(() => message = string.Empty)" />

