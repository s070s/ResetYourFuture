@implements IDisposable

<AuthorizeView>
    <Authorized>
        <div class="avatar-dropdown" @onfocusout="HandleFocusOut" tabindex="-1">
            <button class="avatar-button" @onclick="ToggleDropdown" @onclick:stopPropagation="true"
                    title="@NavMenuRes.UserMenuTitle" aria-label="@NavMenuRes.UserMenuTitle"
                    aria-haspopup="menu" aria-expanded="@(isOpen ? "true" : "false")">
                @if (!string.IsNullOrEmpty(avatarUrl))
                {
                    <img src="@avatarUrl" alt="@NavMenuRes.AvatarAlt" class="avatar-img" />
                }
                else
                {
                    <span class="bi bi-person-circle avatar-icon"></span>
                }
            </button>

            @if (isOpen)
            {
                <div class="dropdown-menu-custom" role="menu">
                    <a class="dropdown-item-custom" role="menuitem" href="profile" @onclick="Close">
                        <span class="bi bi-person" aria-hidden="true"></span> @NavMenuRes.UserProfile
                    </a>
                    @if (_isImpersonating)
                    {
                        <span class="dropdown-item-custom text-light-muted" style="cursor:default;">
                            <span class="bi bi-slash-circle" aria-hidden="true"></span> @NavMenuRes.ImpersonatingHint
                        </span>
                    }
                    else
                    {
                        <button class="dropdown-item-custom" role="menuitem" @onclick="HandleLogout">
                            <span class="bi bi-box-arrow-right" aria-hidden="true"></span> @NavMenuRes.LogOut
                        </button>
                    }
                </div>
            }
        </div>
    </Authorized>
</AuthorizeView>

